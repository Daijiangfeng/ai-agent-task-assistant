# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# LLM & Agent
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-anthropic>=0.3.0
# app/rag/splitter.py 直接依赖 langchain_text_splitters；新版 langchain 不再传递引入，需显式声明。
langchain-text-splitters>=0.3.0
anthropic>=0.39.0
langgraph>=0.2.0
# 任务/检查点持久化：LangGraph Checkpoint 的 PostgreSQL 后端（AsyncPostgresSaver）
langgraph-checkpoint-postgres>=2.0.0

# Database
sqlalchemy[asyncio]>=2.0.0
# 异步驱动：PostgreSQL(asyncpg) / SQLite(aiosqlite)；psycopg 供 pgvector 向量库与 checkpoint 探测
asyncpg>=0.29.0
aiosqlite>=0.20.0
psycopg[binary]>=3.2.0
# pgvector 向量库后端（生产多实例部署时替代 Chroma）
pgvector>=0.3.0

# Redis
redis>=5.0.0

# RAG & Vector Store
chromadb>=0.5.0
pypdf>=5.0.0
python-docx>=1.1.0

# Tools
tavily-python>=0.5.0
# sql_query 工具安全层：SQL AST 校验（表/列权限、危险函数检测）
sqlglot>=25.0.0

# Utils
python-dotenv>=1.0.0
httpx>=0.27.0
tenacity>=9.0.0
structlog>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Lint / Quality gate
ruff>=0.6.0
